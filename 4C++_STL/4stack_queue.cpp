#include <iostream>
#include <stack>
#include <queue>
using namespace std;
void test01()
{
    stack<int> s;
    s.push(10);
    queue<int> q;
    q.push(23);
    q.size;
    q.pop();
    q.empty();
    q.front();

}

int main()
{
    test01();
    return 0;
}